# Story 7.6: Pre-Launch - Archive Tab for Fresh Start Mode

## Status
Done

## Story
**As a** Fresh Start mode user  
**I want to** view my archived tasks in an Archive tab  
**So that** I can still see previous tasks that were sent via email, even though they're cleared from my active list

## Acceptance Criteria
1. Tab bar displays in Fresh Start mode (currently only shows in Carry Over mode)
2. Archive tab queries: `SELECT * FROM tasks WHERE user_id = ? AND status = 'archived' ORDER BY archived_at DESC`
3. Archive tab shows archived tasks with archived timestamp
4. Empty state: "No archived tasks yet! 📧"
5. Archive tab is read-only (no editing, no FAB button)
6. Active tab shows only open tasks (unchanged behavior)

## Tasks / Subtasks

- [x] Task 1: Enable Tab Bar in Fresh Start Mode (AC: 1)
  - [x] **REFERENCE:** `src/screens/MainScreen.tsx` - Tab bar conditional (line 427)
  - [x] Change condition from `workflowMode === 'carry_over'` to always show tab bar
  - [x] Verify tab bar displays for both Fresh Start and Carry Over modes
  - [x] Verify tab bar styling unchanged (44px height, iOS segmented control style)

- [x] Task 2: Implement Archive Query for Fresh Start Mode (AC: 2)
  - [x] **REFERENCE:** `src/screens/MainScreen.tsx` - loadCompletedTasks function (lines 105-148)
  - [x] Create new function: `loadArchivedTasks` (similar to `loadCompletedTasks`)
  - [x] Query archived tasks: `status = 'archived'` instead of `status = 'completed'`
  - [x] Order by `archived_at DESC` instead of `completed_at DESC`
  - [x] Store in new state: `const [archivedTasks, setArchivedTasks] = useState<Task[]>([])`
  - [x] Call `loadArchivedTasks` when `activeTab === 'archive'` and `workflowMode === 'fresh_start'`

- [x] Task 3: Update Tab Content Rendering Logic (AC: 3, 4, 6)
  - [x] **REFERENCE:** `src/screens/MainScreen.tsx` - FlatList data prop (lines 472-476)
  - [x] Update data prop logic:
    - If `activeTab === 'archive'`:
      - Fresh Start: Show `archivedTasks`
      - Carry Over: Show `completedTasks` (existing behavior)
    - If `activeTab === 'active'`: Show `tasks.filter(t => t.status === 'open')` (unchanged)
  - [x] Update empty state logic:
    - Archive tab (Fresh Start): "No archived tasks yet! 📧"
    - Archive tab (Carry Over): Existing "No completed tasks yet! 🎉" (unchanged)
    - Active tab: Existing empty state (unchanged)

- [x] Task 4: Update TaskItem Rendering for Archive (AC: 3, 5)
  - [x] **REFERENCE:** `src/screens/MainScreen.tsx` - renderTaskItem function (lines 355-375)
  - [x] **REFERENCE:** `src/components/TaskItem.tsx` - Archive mode support (lines 136-263)
  - [x] Pass `isArchive={true}` to TaskItem when rendering archived tasks
  - [x] Verify TaskItem displays archived timestamp (uses `archived_at` field)
  - [x] Verify TaskItem is read-only in archive mode (no edit, no complete button)
  - [x] Verify FAB button hidden when `activeTab === 'archive'` (existing logic should handle this)

- [x] Task 5: Handle Archive Tab Switching (AC: 2, 3)
  - [x] **REFERENCE:** `src/screens/MainScreen.tsx` - useEffect for loading completed tasks (lines 98-102)
  - [x] Update useEffect to load archived tasks when:
    - `workflowMode === 'fresh_start'` AND `activeTab === 'archive'`
  - [x] Keep existing logic for Carry Over mode (load completed tasks)
  - [x] Verify switching between Active and Archive tabs works smoothly

## Dev Notes

### Previous Story Insights

**From Story 5.3 (Archive View - Carry Over Mode):**
- Tab bar implementation pattern established (44px height, iOS segmented control style)
- Archive tab query pattern: `status = 'completed'` for Carry Over mode
- Empty state patterns for archive view
- TaskItem archive mode rendering (read-only, timestamp display)

**From Story 5.4 (Email Delivery - Mode-Specific Logic):**
- Fresh Start mode archives tasks after email sent: `status = 'archived'`, `archived_at = NOW()`
- Tasks are archived with `archived_at` timestamp (not `completed_at`)
- Archive happens in Edge Function `send-daily-emails` after successful email send

**From Story 2.1 (Tasks Table & Basic UI):**
- MainScreen structure and FlatList rendering patterns
- Empty state component structure
- Task filtering patterns (`tasks.filter(t => t.status === 'open')`)

### Component Specifications

**MainScreen Component:**
- Located at `src/screens/MainScreen.tsx`
- Current tab bar condition: Line 427 - `{workflowMode === 'carry_over' && (`
- Tab bar shows "Active" and "Archive" tabs
- Active tab shows: `tasks.filter(t => t.status === 'open')`
- Archive tab (Carry Over): Shows `completedTasks` (status = 'completed')

**TaskItem Component:**
- Located at `src/components/TaskItem.tsx`
- Already supports `isArchive` prop (line 14)
- Archive mode displays: Task text + completion/archive timestamp
- Archive mode is read-only (no editing, no complete button)

### Database Schema Context

**Tasks Table - Archive Fields:**
- `status VARCHAR(20) NOT NULL DEFAULT 'open' CHECK (status IN ('open', 'completed', 'archived'))`
- `archived_at TIMESTAMP DEFAULT NULL` - Set when task archived (Fresh Start mode)
- `completed_at TIMESTAMP DEFAULT NULL` - Set when task completed (Carry Over mode)

**Query Pattern for Archive Tab (Fresh Start):**
```typescript
const { data } = await supabase
  .from('tasks')
  .select('*')
  .eq('user_id', user.id)
  .eq('status', 'archived')  // Fresh Start: archived tasks
  .is('deleted_at', null)
  .order('archived_at', { ascending: false });
```

**Query Pattern for Archive Tab (Carry Over):**
```typescript
// Existing - no changes needed
const { data } = await supabase
  .from('tasks')
  .select('*')
  .eq('user_id', user.id)
  .eq('status', 'completed')  // Carry Over: completed tasks
  .is('deleted_at', null)
  .order('completed_at', { ascending: false });
```

### File Locations

**Files to Modify:**
- `src/screens/MainScreen.tsx`
  - Line 427: Change tab bar condition to always show
  - Lines 36-37: Add `archivedTasks` state (similar to `completedTasks`)
  - Lines 98-102: Update useEffect to load archived tasks for Fresh Start mode
  - Lines 105-148: Create `loadArchivedTasks` function (similar to `loadCompletedTasks`)
  - Lines 472-476: Update FlatList data prop to handle Fresh Start archive
  - Lines 493-506: Update empty state logic for Fresh Start archive
  - Lines 355-375: Verify renderTaskItem passes `isArchive` correctly for archived tasks

**Files Referenced (No Changes):**
- `src/components/TaskItem.tsx` - Already supports archive mode (no changes needed)
- `src/stores/taskStore.ts` - Task type definitions (no changes needed)
- `src/lib/supabase.ts` - Supabase client (no changes needed)

### Technical Constraints

**Tab Bar Display:**
- Must show for both Fresh Start and Carry Over modes
- Styling unchanged (44px height, iOS segmented control style)
- Active tab: `colors.primary` background, white text
- Inactive tab: `colors.background` background, `colors.textSecondary` text

**Archive Query:**
- Fresh Start: Query `status = 'archived'`, order by `archived_at DESC`
- Carry Over: Query `status = 'completed'`, order by `completed_at DESC` (unchanged)
- Both queries filter: `user_id`, `deleted_at IS NULL`

**Empty States:**
- Fresh Start Archive: "No archived tasks yet! 📧"
- Carry Over Archive: "No completed tasks yet! 🎉" (unchanged)
- Active tab: Existing empty state (unchanged)

**Read-Only Behavior:**
- Archive tab: No FAB button (existing logic should handle this)
- Archive tasks: No edit, no complete button (TaskItem already handles this via `isArchive` prop)

## Testing

**Manual Test Steps:**

1. **Test Tab Bar Display (AC: 1):**
   - [x] Set user to Fresh Start mode → Verify tab bar displays
   - [x] Set user to Carry Over mode → Verify tab bar displays
   - [x] Verify tab bar shows "Active" and "Archive" tabs
   - [x] Verify tab bar styling matches existing (44px height, iOS segmented control style)

2. **Test Archive Tab Query (AC: 2, 3):**
   - [x] Switch to Fresh Start mode
   - [x] Add some tasks
   - [x] Trigger email send (via Settings test email or wait for daily email)
   - [x] Verify tasks archived (`status = 'archived'`, `archived_at` set)
   - [x] Switch to Archive tab → Verify archived tasks display
   - [x] Verify tasks ordered by `archived_at DESC` (newest first)
   - [x] Verify tasks show archived timestamp

3. **Test Archive Tab Empty State (AC: 4):**
   - [x] Switch to Fresh Start mode
   - [x] Switch to Archive tab with no archived tasks
   - [x] Verify empty state: "No archived tasks yet! 📧"
   - [x] Verify empty state styling matches existing patterns

4. **Test Archive Tab Read-Only (AC: 5):**
   - [x] Switch to Archive tab (Fresh Start mode)
   - [x] Verify no FAB button displays
   - [x] Verify archived tasks are read-only (no edit button, no complete button)
   - [x] Verify task text displays with archived timestamp

5. **Test Tab Switching (AC: 6):**
   - [x] Switch between Active and Archive tabs
   - [x] Verify Active tab shows only open tasks (unchanged)
   - [x] Verify Archive tab shows archived tasks (Fresh Start) or completed tasks (Carry Over)
   - [x] Verify smooth tab switching animation

6. **Test Carry Over Mode Unchanged:**
   - [x] Switch to Carry Over mode
   - [x] Verify Archive tab still shows completed tasks (unchanged behavior)
   - [x] Verify no regression in Carry Over mode functionality
   - [x] Verify tasks remain archived when switching between modes (expected behavior)

**Test Results Summary:**
- ✅ All acceptance criteria verified and passing
- ✅ Tab bar displays correctly in both Fresh Start and Carry Over modes
- ✅ Test email successfully archives tasks in Fresh Start mode
- ✅ Archive tab displays archived tasks with correct timestamps
- ✅ Tab switching works smoothly between Active and Archive tabs
- ✅ Workflow mode switching updates UI immediately (checkboxes appear/disappear correctly)
- ✅ No regressions in Carry Over mode functionality

**Test File Location:**
- No automated tests required for this story (manual verification sufficient)
- Consider adding integration tests in future if testing framework expanded

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-01-27 | 1.0 | Initial story creation - Archive tab for Fresh Start mode | John (PM) |
| 2025-01-27 | 1.1 | Implementation complete - All tasks completed | James (Dev) |
| 2025-01-27 | 1.2 | Bug fixes: Test email now archives tasks in Fresh Start mode, workflow mode reloads when changed in Settings | James (Dev) |

## File List
- `src/screens/MainScreen.tsx` - Modified: Added archivedTasks state, loadArchivedTasks function, updated tab bar display, FlatList data/empty state logic, renderTaskItem, FAB button logic, pull-to-refresh handler, reload workflow mode when Settings closes
- `src/components/TaskItem.tsx` - Modified: Updated formatArchiveDate function to handle both archived_at and completed_at timestamps with appropriate prefixes
- `src/screens/SettingsScreen.tsx` - Modified: Test email function now archives tasks in Fresh Start mode after successful email send, added workflow mode descriptions

## QA Results

### Review Date: 2025-01-27

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

**Overall Assessment:** Excellent implementation quality. The code follows established patterns from Story 5.3 (Archive View - Carry Over Mode), demonstrating good code reuse and consistency. The implementation is clean, well-structured, and maintains separation of concerns.

**Strengths:**
- Consistent pattern reuse: `loadArchivedTasks` mirrors `loadCompletedTasks` structure, ensuring maintainability
- Proper error handling: All async operations include try-catch blocks with appropriate logging
- Type safety: Proper use of TypeScript types and type assertions where needed
- State management: Clean useState hooks for archived tasks, proper dependency arrays
- User experience: Thoughtful implementation including pull-to-refresh support and workflow mode reload on Settings close

**Code Patterns:**
- Follows React Native best practices with proper useEffect dependencies
- Consistent with existing codebase patterns (matches Story 5.3 implementation)
- Good separation: UI logic in MainScreen, formatting logic in TaskItem component

### Refactoring Performed

No refactoring required. Code quality is excellent and follows established patterns.

### Compliance Check

- **Coding Standards:** ✓ Code follows React Native/TypeScript conventions, consistent naming, proper error handling
- **Project Structure:** ✓ Files placed in correct directories (`src/screens/`, `src/components/`)
- **Testing Strategy:** ✓ Manual testing comprehensive, story explicitly allows manual verification (line 219)
- **All ACs Met:** ✓ All 6 acceptance criteria fully implemented and verified through manual testing

### Requirements Traceability

**AC 1 - Tab bar displays in Fresh Start mode:**
- **Given:** User is in Fresh Start mode
- **When:** MainScreen renders
- **Then:** Tab bar displays with "Active" and "Archive" tabs
- **Test Coverage:** Manual test step 1 verified ✓

**AC 2 - Archive tab queries archived tasks:**
- **Given:** User is in Fresh Start mode and Archive tab is active
- **When:** Archive tab is accessed
- **Then:** `loadArchivedTasks` queries `status = 'archived'` ordered by `archived_at DESC`
- **Test Coverage:** Manual test step 2 verified ✓

**AC 3 - Archive tab shows archived tasks with timestamp:**
- **Given:** User has archived tasks
- **When:** Archive tab is viewed
- **Then:** Tasks display with archived timestamp via `formatArchiveDate` function
- **Test Coverage:** Manual test step 2 verified ✓

**AC 4 - Empty state message:**
- **Given:** User has no archived tasks
- **When:** Archive tab is viewed in Fresh Start mode
- **Then:** Empty state displays "No archived tasks yet! 📧"
- **Test Coverage:** Manual test step 3 verified ✓

**AC 5 - Archive tab read-only:**
- **Given:** User is viewing Archive tab
- **When:** Archive tab is active
- **Then:** No FAB button displays, tasks are read-only (no edit/complete buttons)
- **Test Coverage:** Manual test step 4 verified ✓

**AC 6 - Active tab unchanged:**
- **Given:** User is viewing Active tab
- **When:** Active tab is displayed
- **Then:** Only open tasks are shown (unchanged behavior)
- **Test Coverage:** Manual test step 5 verified ✓

### Improvements Checklist

- [x] Verified all acceptance criteria implementation
- [x] Confirmed error handling is comprehensive
- [x] Validated code follows established patterns
- [ ] Consider extracting shared query logic (low priority - future enhancement)
- [ ] Consider adding automated integration tests (future enhancement per story note)

### Security Review

**Status:** PASS

No security concerns identified. All database queries use existing RLS policies and user authentication checks. The implementation only adds UI display logic and query functions that follow the same security patterns as existing `loadCompletedTasks` function.

### Performance Considerations

**Status:** PASS

- Queries are efficient: Uses indexed columns (`user_id`, `status`, `archived_at`)
- Proper ordering: `archived_at DESC` ensures newest tasks appear first
- State management: Minimal re-renders, proper React hooks usage
- No performance degradation: Implementation mirrors existing patterns

### Testability Evaluation

**Controllability:** ✓ High - Can control workflow mode, tab selection, and task archiving via test email
**Observability:** ✓ High - UI changes are clearly visible (tab bar, task display, empty states)
**Debuggability:** ✓ High - Error logging in place, clear code structure, follows established patterns

### Files Modified During Review

No files modified during QA review. Implementation is production-ready.

### Gate Status

**Gate:** PASS → `docs/qa/gates/7.6-pre-launch-archive-tab-fresh-start.yml`

**Quality Score:** 90/100
- Deduction: -10 for no automated tests (but story explicitly allows manual verification)

**Risk Profile:** Low risk - UI extension following established patterns, no security/auth changes

**NFR Assessment:** All NFRs passing (Security: PASS, Performance: PASS, Reliability: PASS, Maintainability: PASS)

### Recommended Status

✓ **Ready for Done** - All acceptance criteria met, comprehensive manual testing completed, code quality excellent, no blocking issues identified.



