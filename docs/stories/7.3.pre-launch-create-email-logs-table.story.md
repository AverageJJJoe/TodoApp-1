# Story 7.3: Pre-Launch - Create Email Logs Table

## Status
Done

## Story
**As a** system administrator  
**I want to** have email delivery logs tracked in the database  
**So that** I can monitor email delivery, debug issues, and maintain an audit trail of all sent emails

## Acceptance Criteria
1. Create `email_logs` table with all required columns per architecture schema
2. Create required indexes: `idx_email_logs_user_sent` on `(user_id, sent_at DESC)`
3. Enable Row-Level Security (RLS) on `email_logs` table
4. Create RLS policy: `email_logs_select_own` allowing users to see only their own email logs
5. Verify Edge Function `send-daily-emails` successfully logs emails after migration deployment
6. Test: Trigger test email → Verify entry created in `email_logs` table with correct data

## Tasks / Subtasks

- [x] Task 1: Create Migration File (AC: 1, 2, 3, 4)
  - [x] Create file: `supabase/migrations/005_email_logs_table.sql`
  - [x] Add migration header comment: Story reference and description
  - [x] Enable UUID extension: `CREATE EXTENSION IF NOT EXISTS "uuid-ossp";`
  - [x] Create `email_logs` table with all columns:
    - `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`
    - `user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE`
    - `recipient_email VARCHAR(255) NOT NULL`
    - `subject VARCHAR(255) NOT NULL`
    - `resend_message_id VARCHAR(255) UNIQUE DEFAULT NULL`
    - `sent_at TIMESTAMP NOT NULL DEFAULT NOW()`
    - `status VARCHAR(20) NOT NULL DEFAULT 'sent' CHECK (status IN ('sent', 'delivered', 'bounced', 'failed'))`
    - `task_count INTEGER NOT NULL`
    - `created_at TIMESTAMP DEFAULT NOW()`
  - [x] Create index: `CREATE INDEX IF NOT EXISTS idx_email_logs_user_sent ON email_logs(user_id, sent_at DESC);`
  - [x] Enable RLS: `ALTER TABLE email_logs ENABLE ROW LEVEL SECURITY;`
  - [x] Create RLS policy: `email_logs_select_own` policy (users can only see their own logs)
  - [x] Use `CREATE TABLE IF NOT EXISTS` pattern (matches existing migrations)
  - [x] Use `DROP POLICY IF EXISTS` before creating policy (matches existing migrations)

- [x] Task 2: Deploy Migration (AC: 5)
  - [x] Deploy migration to Supabase database via SQL Editor or Supabase CLI
  - [x] Verify table created: `SELECT * FROM email_logs LIMIT 1;` (should return empty, no error)
  - [x] Verify index created: `SELECT indexname FROM pg_indexes WHERE tablename = 'email_logs';`
  - [x] Verify RLS enabled: `SELECT tablename, rowsecurity FROM pg_tables WHERE tablename = 'email_logs';`
  - [x] Verify policy created: `SELECT policyname FROM pg_policies WHERE tablename = 'email_logs';`

- [x] Task 3: Test Email Logging (AC: 5, 6)
  - [x] Trigger test email via SettingsScreen "Send Test Email" button OR manually trigger Edge Function
  - [x] Verify email sent successfully
  - [x] Query `email_logs` table: `SELECT * FROM email_logs ORDER BY created_at DESC LIMIT 1;`
  - [x] Verify log entry contains:
    - [x] `user_id` matches test user
    - [x] `recipient_email` matches test user email
    - [x] `subject` matches email subject
    - [x] `resend_message_id` contains Resend message ID (if available)
    - [x] `task_count` matches number of tasks in email
    - [x] `status` is 'sent' or 'failed' appropriately
    - [x] `sent_at` and `created_at` timestamps are recent
  - [x] Verify no errors in Edge Function logs related to `email_logs` insert

## Dev Notes

### Previous Story Insights

**From Story 3.5 (Automated Cron Job):**
- Edge Function `send-daily-emails` already implements email logging code (lines 408-423)
- Code tries to insert into `email_logs` table but fails silently (error logged, doesn't throw)
- Insert pattern already matches architecture specification
- Error handling: `if (insertLogError) { console.error(...); }` - doesn't fail batch processing
- This story completes AC #7 from Story 3.5 which was marked complete but table was never created

**From Story 1.3 (Users Table) & Story 2.1 (Tasks Table):**
- Migration file naming pattern: `{number}_{description}.sql`
- Use `CREATE TABLE IF NOT EXISTS` pattern
- Use `CREATE EXTENSION IF NOT EXISTS "uuid-ossp";` at top
- Include story reference in header comment
- Use `DROP POLICY IF EXISTS` before creating RLS policies

### Database Schema
[Source: `docs/architecture/database-schema-data-model.md` Lines 116-136, 148, 156, 217-225]

**Email Logs Table Schema:**
```sql
CREATE TABLE email_logs (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  
  -- Email details
  recipient_email VARCHAR(255) NOT NULL,
  subject VARCHAR(255) NOT NULL,
  
  -- Sending info
  resend_message_id VARCHAR(255) UNIQUE DEFAULT NULL,
  sent_at TIMESTAMP NOT NULL DEFAULT NOW(),
  status VARCHAR(20) NOT NULL DEFAULT 'sent'
    CHECK (status IN ('sent', 'delivered', 'bounced', 'failed')),
  
  -- Task count for this email
  task_count INTEGER NOT NULL,
  
  -- Timestamps
  created_at TIMESTAMP DEFAULT NOW()
);
```

**Required Index:**
```sql
CREATE INDEX idx_email_logs_user_sent ON email_logs(user_id, sent_at DESC);
```

**RLS Policy:**
```sql
-- Email logs: Users can only see their own logs (for debugging)
CREATE POLICY "email_logs_select_own" ON email_logs
  FOR SELECT USING (
    EXISTS (
      SELECT 1 FROM users 
      WHERE users.id = email_logs.user_id 
      AND users.auth_id = auth.uid()
    )
  );
```

### File Locations

**Files to Create:**
- `supabase/migrations/005_email_logs_table.sql` - New migration file

**Files Referenced (No Changes):**
- `supabase/functions/send-daily-emails/index.ts` - Already implements logging (lines 408-423)
- `src/screens/SettingsScreen.tsx` - Can be used to trigger test email

### Technical Constraints

**Migration File Format:**
- Follow existing migration file pattern from `001_users_table.sql` and `002_tasks_table.sql`
- Include story reference in header comment
- Use `CREATE TABLE IF NOT EXISTS` to allow safe re-running
- Use `DROP POLICY IF EXISTS` before creating policies
- Use `CREATE INDEX IF NOT EXISTS` for indexes

**RLS Policy Pattern:**
- Match pattern from `003_users_insert_policy.sql` and `002_tasks_table.sql`
- Use `EXISTS` subquery pattern for checking user ownership via `users.auth_id = auth.uid()`
- Policy name: `email_logs_select_own`
- Only SELECT policy needed (INSERT done by Edge Function with service role key, bypasses RLS)

**Edge Function Integration:**
- Edge Function uses service role key, so INSERT operations bypass RLS
- RLS policy only needed for SELECT (when users query their own logs in future features)
- No changes needed to Edge Function code - it already implements correct insert pattern

### Security Considerations
[Source: `docs/architecture/database-schema-data-model.md` RLS policies]

**RLS Policy:**
- Users can only see their own email logs (via `email_logs_select_own` policy)
- Edge Function uses service role key for INSERT, bypasses RLS (appropriate for automated email logging)
- Foreign key constraint ensures `user_id` references valid user
- Cascade delete: When user is deleted, their email logs are automatically deleted

**No Security Impact:**
- Table creation is safe - no sensitive data exposure
- RLS properly restricts user access to their own logs only
- Service role key usage is appropriate for automated system operations

## Testing

**Manual Test Steps:**
1. **Verify Migration Deployment:**
   - [ ] Deploy migration file to Supabase database
   - [ ] Run: `SELECT * FROM email_logs LIMIT 1;` (should return empty result, no error)
   - [ ] Verify table exists: `SELECT tablename FROM pg_tables WHERE tablename = 'email_logs';`
   - [ ] Verify index exists: `SELECT indexname FROM pg_indexes WHERE tablename = 'email_logs' AND indexname = 'idx_email_logs_user_sent';`
   - [ ] Verify RLS enabled: `SELECT tablename, rowsecurity FROM pg_tables WHERE tablename = 'email_logs';` (should show `rowsecurity = true`)
   - [ ] Verify policy exists: `SELECT policyname FROM pg_policies WHERE tablename = 'email_logs';` (should show `email_logs_select_own`)

2. **Test Email Logging:**
   - [ ] Use SettingsScreen "Send Test Email" button OR manually trigger Edge Function
   - [ ] Verify email received successfully
   - [ ] Query logs: `SELECT * FROM email_logs ORDER BY created_at DESC LIMIT 1;`
   - [ ] Verify all fields populated correctly:
     - [ ] `user_id` matches test user ID
     - [ ] `recipient_email` matches test user email
     - [ ] `subject` matches email subject (e.g., "Your 3 Todos for Today")
     - [ ] `resend_message_id` contains Resend message ID (or NULL if send failed)
     - [ ] `task_count` matches number of tasks included in email
     - [ ] `status` is 'sent' (if message_id present) or 'failed' (if NULL)
     - [ ] `sent_at` timestamp is recent (within last minute)
     - [ ] `created_at` timestamp is recent (within last minute)

3. **Test RLS Policy:**
   - [ ] As authenticated user, query: `SELECT * FROM email_logs WHERE user_id = '<current_user_id>';`
   - [ ] Verify only user's own logs are returned
   - [ ] Verify no access to other users' logs

4. **Verify Edge Function Logs:**
   - [ ] Check Supabase Edge Functions logs for `send-daily-emails`
   - [ ] Verify no errors related to `email_logs` insert
   - [ ] Verify successful insert messages (if logged)

**Test File Location:**
- No automated tests required for this story (database migration, manual verification sufficient)

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-11-04 | 1.0 | Initial story creation - Fix missing email_logs table from Story 3.5 | Bob (SM) |
| 2025-11-04 | 1.1 | Story validated and approved for development | Sarah (PO) |

## Dev Agent Record

### Agent Model Used
Claude Sonnet 4.5

### Debug Log References
_No debug log entries required_

### Completion Notes List
- Task 1 completed: Created migration file `supabase/migrations/005_email_logs_table.sql` following existing migration patterns
- Migration includes all required columns, index, RLS enablement, and RLS policy matching architecture specification
- File validated: No linting errors, follows established patterns from `001_users_table.sql` and `002_tasks_table.sql`
- Task 2 completed: Migration successfully deployed via Supabase Dashboard SQL Editor. Execution returned "Success. No rows returned" confirming DDL statements executed successfully. Table, index, RLS, and policy created.
- Task 3 completed: Email logging tested and verified. Edge Function `send-daily-emails` successfully logs emails to `email_logs` table. Test email sent via SettingsScreen confirmed log entry created with correct data (user_id, recipient_email, subject, resend_message_id, task_count, status, timestamps).

### File List
- Created: `supabase/migrations/005_email_logs_table.sql`

## QA Results

### Review Date: 2025-01-27

### Reviewed By: Bob (Scrum Master)

### Code Quality Assessment

**Overall Assessment:** ✅ **COMPLETE**

Migration file is correctly structured, follows established patterns, and has been successfully deployed. Email logging functionality is working as expected.

### Compliance Check

- **Coding Standards:** ✅ PASS - SQL follows project migration patterns
- **Project Structure:** ✅ PASS - Migration file in correct location
- **Testing Strategy:** ✅ PASS - Manual testing completed and verified
- **All ACs Met:** ✅ PASS - All 6 acceptance criteria fully implemented and tested

### Recommended Status

✅ **Ready for Done** - Migration deployed, tested, and verified. Story complete.

