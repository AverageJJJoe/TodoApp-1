# Story 8.5: Loops Re-Engagement Email Sequences (Optional)

## Status
Approved

**Approved:** 2025-01-28  
**Ready for Development:** ✅ Yes (Optional - can be implemented after Story 8.4)  
**Story Checklist:** ✅ PASS (All criteria met)  
**PO Validation:** ✅ PASS (Ready for implementation)

## Epic Context
**Epic 8: Sentry Crash Logging, PostHog Analytics & Loops Email Sequences - Post-Launch Enhancement**
This story is part of Epic 8, a post-launch epic focused on integrating Sentry for crash logging, PostHog for product analytics, and Loops for lifecycle/marketing emails. This story implements automated re-engagement email sequences for inactive users.

**Dependencies:** Story 8.4 complete (Loops API integration for welcome emails)

**Decision:** Optional post-launch enhancement. Can be implemented using Loops' built-in automation features or simple Edge Functions (no database triggers needed).

**Email Provider Strategy:**
- **Resend:** Handles transactional emails (magic links via SMTP, daily digests via API) - CRITICAL, must be reliable
- **Loops:** Handles lifecycle/marketing emails (welcome, re-engagement) - NON-CRITICAL, nice-to-have

## Story
**As a** product manager  
**I want** automated re-engagement email sequences for inactive users  
**So that** I can bring back users who haven't used the app in 3-5 days and improve retention

## Acceptance Criteria

**Option A: Use Loops Built-in Automation (Recommended - Zero Code)**
1. Check Loops dashboard for "Email Sequences" or "Automation" features
2. Create email sequence triggered by user signup date or last activity:
   - Day 3: "No tasks" nudge email (if user has no tasks created)
   - Day 5: Inactivity re-engagement email (if user hasn't created task in 5 days)
3. Configure Loops to track user activity via Loops API events (sent from app when tasks are created)
4. Loops automatically sends emails based on sequence rules

**Option B: Simple Edge Function Approach (If Loops Automation Not Available)**
1. ✅ Loops email templates created:
   - ✅ "Day 3 No Tasks" template (Transactional ID: `cmhvyaekx3ttwwq0i7b9ewfq7`)
   - ✅ "Day 5 Inactivity" template (Transactional ID: `cmhw3sebk0bqf760ipe4mvthm`)
   - ✅ Templates published and ready to use

2. Supabase Edge Function created: `send-loops-reengagement-emails`
   - Runs daily (via Supabase cron job or scheduled Edge Function)
   - Queries users who match criteria:
     - Day 3: Users who signed up 3 days ago with no tasks created
     - Day 5: Users who haven't created a task in 5 days
   - Calls Loops API to send appropriate re-engagement email
   - Uses Loops API: `POST https://app.loops.so/api/v1/transactional`
   - Tracks last email sent to prevent duplicates (store timestamp in user metadata or separate table)
   - Handles errors gracefully (logs but doesn't break cron job)

3. Edge Function scheduled to run daily at 9 AM UTC

4. Re-engagement emails include:
   - Friendly reminder about TodoTomorrow
   - Link to open app
   - Contextual message based on user state (no tasks vs inactive)

5. Edge Function logs all actions for debugging

**Testing (If Implemented):**
- Create test user, wait 3 days with no tasks → Verify Day 3 "no tasks" email sent
- Create test user, wait 5 days without activity → Verify Day 5 inactivity email sent
- Check Loops dashboard → Re-engagement emails tracked
- Verify no duplicate emails sent to same user
- Verify users who become active are removed from re-engagement queue

## Tasks / Subtasks

- [x] Task 1: Evaluate Loops Built-in Automation (AC: Option A)
  - [x] Check Loops dashboard for "Email Sequences" or "Automation" features
  - [x] Review Loops documentation for automation capabilities
  - [x] If available, proceed with Option A (zero code approach)
  - [x] If not available, proceed with Option B (Edge Function approach)
  - [x] **Decision:** Proceeding with Option B (Edge Function approach) - more control and easier to test/debug

- [x] Task 2: Create Edge Function for Re-Engagement Emails (AC: Option B, 2)
  - [x] Create directory: `supabase/functions/send-loops-reengagement-emails/`
  - [x] Create `index.ts` file following pattern from `send-daily-emails` Edge Function
  - [x] Implement Edge Function handler:
    - [x] Get `LOOPS_API_KEY` from environment variables (`Deno.env.get('LOOPS_API_KEY')`)
    - [x] Query users for Day 3 criteria:
      - [x] Users who signed up exactly 3 days ago (`created_at::date = NOW()::date - INTERVAL '3 days'`)
      - [x] Users with no tasks created (query tasks table, check if count = 0)
      - [x] Users who haven't received Day 3 email yet (check `last_day3_email_sent_at IS NULL`)
    - [x] Query users for Day 5 criteria:
      - [x] Users who haven't created a task in 5 days:
        - [x] **Option 1 (Recommended - No Schema Change):** Query `tasks` table: `SELECT MAX(created_at) FROM tasks WHERE user_id = ?` - if NULL or < NOW() - INTERVAL '5 days', user is eligible
        - [x] **Decision:** Using Option 1 (no schema change required)
      - [x] Users who haven't received Day 5 email yet (check `last_day5_email_sent_at IS NULL`)
    - [x] For each eligible user:
      - [x] Call Loops API to send appropriate email:
        - [x] Day 3: Use transactional ID `cmhvyaekx3ttwwq0i7b9ewfq7`
        - [x] Day 5: Use transactional ID `cmhw3sebk0bqf760ipe4mvthm`
      - [x] Update user record to track email sent:
        - [x] Day 3: Set `last_day3_email_sent_at = NOW()`
        - [x] Day 5: Set `last_day5_email_sent_at = NOW()`
    - [x] Handle errors gracefully (log but don't break cron job)
    - [x] Return summary of emails sent (for logging)

- [x] Task 3: Add Email Tracking Fields to Users Table (AC: Option B, 2)
  - [x] Check existing migrations to find highest migration number (015)
  - [x] Create migration file: `supabase/migrations/016_add_reengagement_email_tracking.sql`
  - [x] Add columns to `users` table:
    - [x] `last_day3_email_sent_at TIMESTAMP WITH TIME ZONE NULL`
    - [x] `last_day5_email_sent_at TIMESTAMP WITH TIME ZONE NULL`
  - [x] Add indexes for query performance:
    - [x] Index on `created_at` for Day 3 queries
    - [x] Index on `last_day3_email_sent_at` for Day 3 queries
    - [x] Index on `last_day5_email_sent_at` for Day 5 queries
  - [x] Add comments explaining purpose of each column
  - [ ] Test migration in local Supabase instance (requires migration deployment)

- [x] Task 4: Schedule Edge Function via Cron Job (AC: Option B, 3)
  - [x] Verify Supabase cron job setup method:
    - [x] Check if `pg_cron` extension is enabled (already enabled from Story 3.5)
    - [x] Reference Story 3.5 (Automated Cron Job) for existing cron job pattern
  - [x] Create cron job migration: `supabase/migrations/017_cron_job_send_loops_reengagement_emails.sql`
    - [x] **Using pg_cron:** Create cron job via SQL migration:
      - [x] Job name: `send-loops-reengagement-emails-daily`
      - [x] Schedule: Daily at 9 AM UTC (`0 9 * * *`)
      - [x] Function URL: `https://zrnjxrtgrommlhexbpde.supabase.co/functions/v1/send-loops-reengagement-emails`
      - [x] Uses `net.http_post()` with schema prefix (following Story 3.5 pattern)
  - [x] Document cron job schedule, function name, and setup method used
  - [x] Deploy migration to create cron job (migration deployed successfully)

- [x] Task 5: Store Loops API Key in Supabase Secrets (AC: Option B, 2)
  - [x] Get Loops API key from Loops dashboard → Settings → API (already obtained: `b937e28467128929b051ae7969efc98a`)
  - [x] Store in Supabase secrets: Added via Supabase Dashboard → Settings → Edge Functions → Secrets
  - [x] Verify API key is accessible in Edge Function via `Deno.env.get('LOOPS_API_KEY')` (Edge Function deployed, API key configured)
  - [x] Document API key location for future reference (Supabase secrets, different from EAS secrets)

- [ ] Task 6: Testing (AC: Testing)
  - [ ] Test Day 3 email:
    - [ ] Create test user with `created_at = NOW() - INTERVAL '3 days'`
    - [ ] Verify user has no tasks
    - [ ] Manually trigger Edge Function or wait for cron job
    - [ ] Verify Day 3 email received via Loops
    - [ ] Check Loops dashboard → Metrics → Verify email sent/delivered
    - [ ] Verify `last_day3_email_sent_at` updated in database
  - [ ] Test Day 5 email:
    - [ ] Create test user with last task created 5+ days ago
    - [ ] Manually trigger Edge Function or wait for cron job
    - [ ] Verify Day 5 email received via Loops
    - [ ] Check Loops dashboard → Metrics → Verify email sent/delivered
    - [ ] Verify `last_day5_email_sent_at` updated in database
  - [ ] Test duplicate prevention:
    - [ ] Create test user eligible for Day 3 email
    - [ ] Trigger Edge Function twice
    - [ ] Verify only one email sent (second run should skip user)
  - [ ] Test active user exclusion:
    - [ ] Create test user eligible for Day 3 email
    - [ ] Create a task for that user
    - [ ] Trigger Edge Function
    - [ ] Verify Day 3 email NOT sent (user has tasks now)
  - [ ] Test error handling:
    - [ ] Temporarily set invalid Loops API key
    - [ ] Trigger Edge Function
    - [ ] Verify cron job doesn't crash (errors logged but function completes)

## Dev Notes

### Previous Story Insights
- Story 8.4 (Loops Welcome Email): Loops API integration pattern established in `src/lib/loops.ts`
- Story 3.5 (Automated Cron Job): Pattern for scheduled Edge Functions and cron jobs
- Story 5.3 (Email Delivery): Edge Function pattern from `send-daily-emails` function

### Email Provider Strategy
- **Resend (Transactional - CRITICAL):**
  - Magic link authentication (via Supabase SMTP) - MUST be reliable
  - Daily todo digest emails (via Edge Functions API) - Core product feature
- **Loops (Lifecycle/Marketing - NON-CRITICAL):**
  - Welcome emails (via Loops API) - Story 8.4 ✅
  - Re-engagement emails (via Loops API) - This story

**Rationale:** Re-engagement emails are marketing/engagement emails. If Loops has issues, core product still works.

### Loops API Details
- **Endpoint:** `POST https://app.loops.so/api/v1/transactional`
- **Authentication:** Bearer token (Loops API key)
- **Day 3 No Tasks Template ID:** `cmhvyaekx3ttwwq0i7b9ewfq7`
- **Day 5 Inactivity Template ID:** `cmhw3sebk0bqf760ipe4mvthm`
- **Request Format:**
  ```json
  {
    "transactionalId": "cmhvyaekx3ttwwq0i7b9ewfq7",
    "email": "user@example.com"
  }
  ```
- **Reference Documentation:**
  - [Loops API Documentation](https://loops.so/docs/api)
  - [Loops Transactional Emails](https://loops.so/docs/transactional)

### Database Schema
- **Users Table:** Already exists, need to add tracking columns
- **New Columns:**
  - `last_day3_email_sent_at TIMESTAMP WITH TIME ZONE NULL` - Tracks when Day 3 email was sent
  - `last_day5_email_sent_at TIMESTAMP WITH TIME ZONE NULL` - Tracks when Day 5 email was sent
- **Indexes:** Add indexes on `created_at`, `last_day3_email_sent_at`, `last_day5_email_sent_at` for query performance

### File Locations
- **New File:** `supabase/functions/send-loops-reengagement-emails/index.ts` - Edge Function for re-engagement emails
- **New Migration:** `supabase/migrations/[NEXT_SEQUENTIAL_NUMBER]_add_reengagement_email_tracking.sql` - Add tracking columns to users table (check existing migrations for highest number)
- **Reference:** `supabase/functions/send-daily-emails/index.ts` - Pattern for Edge Function structure
- **Reference:** Story 3.5 (Automated Cron Job) - Pattern for pg_cron setup with `net.http_post()`

### Implementation Approach
- **Query Logic:**
  - Day 3: Users who signed up 3 days ago (`created_at::date = (NOW() - INTERVAL '3 days')::date`) with no tasks
  - Day 5: Users who haven't created a task in 5 days:
    - **Recommended Approach:** Query `tasks` table: `SELECT MAX(created_at) FROM tasks WHERE user_id = ?` - if NULL or < NOW() - INTERVAL '5 days', user is eligible
    - **Alternative (If Performance Requires):** Add `last_task_created_at` column to `users` table and update on task creation
- **Duplicate Prevention:** Check `last_day3_email_sent_at` and `last_day5_email_sent_at` before sending
- **Error Handling:** Wrap Loops API calls in try-catch, log errors but don't break cron job
- **Non-blocking:** Re-engagement emails are nice-to-have, shouldn't break cron job if Loops API fails
- **Cron Job Setup:** Verify Supabase cron method (pg_cron extension vs scheduled Edge Functions) - reference Story 3.5 for existing pattern

### Testing Standards
- **Test Location:** Manual testing in development environment, then production monitoring
- **Test Approach:**
  - Create test users with specific dates (3 days ago, 5+ days inactive)
  - Manually trigger Edge Function or wait for cron job
  - Verify emails received and database updated
  - Test duplicate prevention and active user exclusion
- **Success Criteria:** Emails sent correctly, no duplicates, active users excluded, cron job doesn't crash

### Technical Constraints
- Loops API key must be stored in Supabase secrets (not hardcoded)
- Edge Function must handle errors gracefully (cron job shouldn't fail)
- Day 5 query approach: Prefer querying `tasks` table (no schema change) unless performance requires `last_task_created_at` column
- Cron job setup: Verify method (pg_cron vs scheduled Edge Functions) - reference Story 3.5 for existing pattern
- Cron job runs daily at 9 AM UTC (adjust if needed for timezone considerations)
- Migration numbering: Use next sequential number (check existing migrations for highest number)

### Rollback Plan
If issues arise:
1. Disable cron job in Supabase Dashboard
2. Optionally remove Edge Function: `supabase/functions/send-loops-reengagement-emails/`
3. Optionally revert migration (remove tracking columns from users table)
4. Re-engagement emails stop, but core product functionality unaffected

### Optional: Loops Built-in Automation
If Loops supports built-in automation:
- Use Loops dashboard to create email sequences
- Configure triggers based on user signup date or activity
- Send Loops API events from app when tasks are created (to track activity)
- Zero code required (dashboard-only configuration)

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-01-28 | 1.0 | Story created based on updated Epic 8 PRD | Scrum Master |
| 2025-01-28 | 1.1 | Story approved and ready for dev team handoff | Scrum Master |

## Dev Agent Record

### Agent Model Used
Composer (Cursor AI)

### Debug Log References
N/A - No debug issues encountered

### Completion Notes List
- ✅ Created Edge Function: `supabase/functions/send-loops-reengagement-emails/index.ts`
- ✅ Implemented Day 3 email logic: Users who signed up 3 days ago with no tasks
- ✅ Implemented Day 5 email logic: Users who haven't created a task in 5 days (queries tasks table, no schema change)
- ✅ Created migration 016: Adds `last_day3_email_sent_at` and `last_day5_email_sent_at` columns to users table
- ✅ Created migration 017: Sets up pg_cron job to run daily at 9 AM UTC
- ✅ Error handling: Graceful error handling, logs errors but doesn't break cron job
- ⚠️ Task 5 (Supabase secrets): Requires manual setup: `supabase secrets set LOOPS_API_KEY=b937e28467128929b051ae7969efc98a`
- ✅ Migrations deployed: Migration 016 (tracking columns) and 017 (cron job) deployed successfully
- ✅ Edge Function deployed: `send-loops-reengagement-emails` deployed and active
- ✅ Loops API key configured in Supabase secrets
- ⚠️ Task 6 (Testing): Ready for manual testing - cron job will run daily at 9 AM UTC

### File List
- **New:** `supabase/functions/send-loops-reengagement-emails/index.ts` - Edge Function for re-engagement emails
- **New:** `supabase/migrations/016_add_reengagement_email_tracking.sql` - Adds tracking columns to users table
- **New:** `supabase/migrations/017_cron_job_send_loops_reengagement_emails.sql` - Creates daily cron job

## QA Results

### Review Date: 2025-11-13

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

**Overall Assessment: GOOD** ✅

The implementation demonstrates solid engineering practices:
- **Edge Function Structure**: Well-organized following established patterns from Story 3.5
- **Error Handling**: Comprehensive try-catch blocks with graceful error collection
- **Database Design**: Proper use of tracking columns with indexes for performance
- **Cron Job Setup**: Correctly configured using pg_cron following existing patterns
- **Code Clarity**: Well-documented with comments explaining logic
- **Query Optimization**: Efficient queries with proper date filtering and null checks

**Architecture Alignment:**
- Follows established Edge Function patterns from `send-daily-emails`
- Consistent with project structure and coding standards
- Proper use of Supabase service role for database operations
- Correct implementation of pg_cron job pattern

### Refactoring Performed

**No refactoring required** - Code quality is acceptable for production.

**Observations:**
- Edge Function properly handles CORS for manual testing
- Error collection pattern allows partial success (some emails sent even if others fail)
- Day 5 query logic correctly handles both users with tasks and users without tasks

### Compliance Check

- **Coding Standards:** ✅ PASS - Follows TypeScript/Deno best practices, consistent naming
- **Project Structure:** ✅ PASS - Files placed in correct locations (`supabase/functions/`, `supabase/migrations/`)
- **Testing Strategy:** ⚠️ PARTIAL - Implementation complete but testing pending (Task 6)
- **All ACs Met:** ⚠️ PARTIAL - Implementation complete, testing required to verify

### Requirements Traceability

**AC Option B.1 - Email Templates:** ✅ VERIFIED
- Templates created and published in Loops dashboard
- Template IDs documented and used correctly

**AC Option B.2 - Edge Function:** ✅ VERIFIED
- Edge Function created with proper structure
- Day 3 logic: Users signed up 3 days ago with no tasks
- Day 5 logic: Users inactive for 5+ days (handles both with/without tasks)
- Loops API integration implemented correctly
- Duplicate prevention via tracking columns
- Error handling graceful (doesn't break cron job)

**AC Option B.3 - Cron Job:** ✅ VERIFIED
- Cron job configured via pg_cron migration
- Scheduled for daily 9 AM UTC
- Uses correct `net.http_post()` pattern

**AC Option B.4 - Email Content:** ✅ VERIFIED (via template review)
- Templates include friendly reminders and app links
- Contextual messages based on user state

**AC Option B.5 - Logging:** ✅ VERIFIED
- Comprehensive logging throughout Edge Function
- Error details captured in result object

**AC Testing:** ⚠️ PENDING
- Task 6 testing not yet completed
- Requires manual testing with test users and time-based scenarios

### Security Review

✅ **PASS** - No security concerns identified
- API key stored securely in Supabase secrets (not hardcoded)
- Service role key used appropriately for Edge Function
- No sensitive data exposed in logs
- Proper authentication headers for Edge Function calls

### Performance Considerations

✅ **PASS** - Performance is acceptable
- Database queries optimized with indexes
- Efficient date filtering reduces query scope
- Batch processing handles multiple users
- Cron job runs once daily (minimal overhead)

**Potential Optimization:**
- Day 5 query fetches all users without `last_day5_email_sent_at`, then filters in code. For large user bases, consider adding date-based filtering in SQL query.

### Testability Evaluation

⚠️ **MODERATE** - Testable but requires time-based scenarios
- **Controllability:** ⚠️ Requires test data setup with specific dates
- **Observability:** ✅ Comprehensive logging enables debugging
- **Debuggability:** ✅ Clear error messages and result summaries
- **Isolation:** ✅ Edge Function can be tested independently

**Testing Challenges:**
- Requires waiting 3-5 days or manipulating database timestamps
- Cron job testing requires manual triggering or waiting for scheduled time
- Edge Function can be manually invoked for testing

### Technical Debt Assessment

**Minimal Technical Debt** ✅

**Identified Items:**
1. **Minor:** Day 5 query could be optimized for large user bases
   - **Impact:** Low - Current approach works, optimization only needed at scale
   - **Recommendation:** Monitor query performance, optimize if user base grows significantly

2. **Testing Gap:** Task 6 testing not yet completed
   - **Impact:** Medium - Functionality unverified in production-like environment
   - **Recommendation:** Complete testing before marking story as Done

### Code Review Findings

**Positive Aspects:**
- ✅ Proper error handling with result collection
- ✅ Correct use of Supabase service role
- ✅ Efficient duplicate prevention logic
- ✅ Well-structured query logic for both Day 3 and Day 5
- ✅ Comprehensive logging for debugging

**Areas for Future Enhancement:**
- Consider adding retry logic for failed Loops API calls
- Monitor Edge Function execution time as user base grows
- Consider adding metrics/analytics for email send rates

### Gate Status

**Gate: CONCERNS** → `docs/qa/gates/8.5-loops-reengagement-email-sequences.yml`

**Quality Score: 85/100** (Deducted 15 points for incomplete testing)

**Rationale:**
- Implementation is complete and code quality is good
- All acceptance criteria implemented correctly
- Security and performance validated
- **CONCERNS:** Testing (Task 6) not yet completed - functionality unverified in production-like environment
- Code is production-ready but requires testing verification

### Recommended Status

⚠️ **Changes Required** - Complete Task 6 (Testing) before marking as Done.

**Testing Requirements:**
1. Create test users with appropriate dates
2. Manually trigger Edge Function or wait for cron job
3. Verify Day 3 and Day 5 emails sent correctly
4. Verify duplicate prevention working
5. Verify active users excluded correctly
6. Verify error handling doesn't break cron job

**After Testing Completion:**
- Re-review to verify all test scenarios pass
- Update gate status to PASS if all tests successful

